.next
node_modules
out
coverage
*.tsbuildinfo
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.venv
*.egg-info
*.db
*.sqlite
*.sqlite3
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Internal SF1 back-test cache — raw licensed Sharadar rows stay internal (governance)
backend/.sf1_cache/

# SBA FOIA loan-level cache — large public downloads stay out of version control
backend/.sba_cache/

# Document Review uploads — sensitive user tax/bank docs stay out of version control
backend/app/data/document_review/

# Domain registration — never commit registrant PII or generated request payloads
scripts/domains/contact.json
scripts/domains/*.register.json
scripts/domains/operations.log

# Auto-loaded Compose override is a sandbox-only DNS hack; never commit it.
# The reusable sandbox helper lives in docker-compose.sandbox.yml (explicit -f).
docker-compose.override.yml
